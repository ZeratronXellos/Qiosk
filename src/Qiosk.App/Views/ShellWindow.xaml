<Window x:Class="Qiosk.App.Views.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:infra="clr-namespace:Qiosk.App.Infrastructure"
        mc:Ignorable="d"
        Title="Qiosk"
        WindowStyle="None"
        WindowState="Maximized"
        ResizeMode="NoResize"
        Background="{StaticResource SurfaceBrush}"
        Loaded="OnLoaded"
        Closing="OnClosing">
    <Window.InputBindings>
        <KeyBinding Key="Q" Modifiers="Control" Command="{Binding ExitApplicationCommand}" />
        <KeyBinding Key="R" Modifiers="Control" Command="{Binding ReloadExcelCommand}" />
    </Window.InputBindings>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <infra:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisibility" />
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FFF5F7FA" Offset="0" />
                <GradientStop Color="#FFE9EFFB" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>

        <Grid Margin="60">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,32">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="48" Height="48" CornerRadius="12" Background="{StaticResource PrimaryBrush}" VerticalAlignment="Center">
                        <TextBlock Text="Q" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel Margin="12,0,0,0">
                        <TextBlock Text="Qiosk" FontSize="32" FontWeight="Bold" />
                        <TextBlock Text="Partenerul tau digital pentru inregistrare rapida" FontSize="18" Foreground="{StaticResource TextSecondaryBrush}" />
                    </StackPanel>
                </StackPanel>
                <Button Grid.Column="1"
                        Content="Admin"
                        Height="52"
                        Padding="28,0"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding OpenAdminCommand}" />
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Padding="36">
                    <StackPanel>
                        <TextBlock Text="Bine ai venit!" FontSize="40" FontWeight="Bold" Margin="0,0,0,8" />
                        <TextBlock Text="Apropie codul QR de camera pentru a-ti genera ecusonul." FontSize="22" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" />

                        <StackPanel Orientation="Horizontal" Margin="0,28,0,0">
                            <Border Width="64" Height="64" CornerRadius="20" Background="#FFEFF5FF" BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="1">
                                <TextBlock Text="1" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <StackPanel Margin="18,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Scaneaza codul" FontSize="22" FontWeight="SemiBold" />
                                <TextBlock Text="Centreaza codul QR in fereastra camerei." FontSize="18" Foreground="{StaticResource TextSecondaryBrush}" />
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,18,0,0">
                            <Border Width="64" Height="64" CornerRadius="20" Background="#FFEFF5FF" BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="1">
                                <TextBlock Text="2" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <StackPanel Margin="18,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Confirma datele" FontSize="22" FontWeight="SemiBold" />
                                <TextBlock Text="Verifica informatiile pe ecran si aproba." FontSize="18" Foreground="{StaticResource TextSecondaryBrush}" />
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,18,0,0">
                            <Border Width="64" Height="64" CornerRadius="20" Background="#FFEFF5FF" BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="1">
                                <TextBlock Text="3" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <StackPanel Margin="18,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Ridica ecusonul" FontSize="22" FontWeight="SemiBold" />
                                <TextBlock Text="Badge-ul tau va fi imprimat imediat." FontSize="18" Foreground="{StaticResource TextSecondaryBrush}" />
                            </StackPanel>
                        </StackPanel>

                        <Border Margin="0,28,0,0"
                                Padding="20"
                                CornerRadius="16"
                                Background="#FFF0F4FF"
                                BorderBrush="#FFCBD5F5"
                                BorderThickness="1">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Ellipse Width="18" Height="18" Fill="{StaticResource PrimaryBrush}" VerticalAlignment="Center" />
                                <StackPanel Margin="16,0,0,0">
                                    <TextBlock Text="Scanner activ" FontSize="20" FontWeight="SemiBold" />
                                    <TextBlock Text="Confirma inainte de imprimare." FontSize="18" Foreground="{StaticResource TextSecondaryBrush}" />
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <Border Margin="0,24,0,0"
                                Padding="18"
                                CornerRadius="14"
                                Visibility="{Binding IsStatusVisible, Converter={StaticResource BoolToVisibility}}">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#FFE8F0FF" />
                                    <Setter Property="BorderBrush" Value="#FFCBD5F5" />
                                    <Setter Property="BorderThickness" Value="1" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsStatusError}" Value="True">
                                            <Setter Property="Background" Value="#FFFFEAEA" />
                                            <Setter Property="BorderBrush" Value="#FFF5B5B8" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding StatusMessage}" FontSize="18" />
                        </Border>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}" Padding="32" Margin="32,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="Previzualizare camera" FontSize="24" FontWeight="SemiBold" />
                        <Border Grid.Row="1" CornerRadius="24" Background="#FFEFF3FA" BorderBrush="#FFCBD5E1" BorderThickness="1">
                            <Grid>
                                <Image Source="{Binding CameraFrame}" Stretch="UniformToFill" />
                                <TextBlock Text="Fara imagine"
                                           FontSize="18"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding CameraFrame}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>
                        <StackPanel Grid.Row="2" Margin="0,24,0,0">
                            <TextBlock Text="Asigura-te ca lampa camerei este libera." FontSize="18" Foreground="{StaticResource TextSecondaryBrush}" />
                            <TextBlock Text="Distanta recomandata: 30-40 cm." FontSize="18" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,8,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <Border Background="#CCF1F4FF"
                Visibility="{Binding IsConfirmationVisible, Converter={StaticResource BoolToVisibility}}">
            <Border HorizontalAlignment="Center" VerticalAlignment="Center" Width="640" Style="{StaticResource CardBorderStyle}" Padding="40">
                <StackPanel>
                    <TextBlock Text="Verifica datele" FontSize="34" FontWeight="Bold" Margin="0,0,0,8" />
                    <TextBlock Text="Confirma informatiile de mai jos pentru tiparirea ecusonului." TextWrapping="Wrap" FontSize="20" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,24" />

                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="Nume" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBlock Text="{Binding CurrentAttendee.FullName}" FontSize="30" FontWeight="SemiBold" />
                    </StackPanel>
                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="Rol" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBlock Text="{Binding CurrentAttendee.Role}" FontSize="24" />
                    </StackPanel>
                    <StackPanel Margin="0,0,0,28">
                        <TextBlock Text="Companie" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBlock Text="{Binding CurrentAttendee.Company}" FontSize="24" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Nu sunt eu" Command="{Binding CancelCurrentCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,16,0" />
                        <Button Content="Confirma" IsDefault="True" Command="{Binding ConfirmCurrentCommand}" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>

        <Border Background="#B3F1F4FF"
                Visibility="{Binding IsPrintingVisible, Converter={StaticResource BoolToVisibility}}">
            <Border HorizontalAlignment="Center" VerticalAlignment="Center" Width="520" Style="{StaticResource CardBorderStyle}" Padding="40">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="Se printeaza ecusonul" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" />
                    <TextBlock Text="Te rugam asteapta cateva secunde." FontSize="20" HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,16,0,0" />
                </StackPanel>
            </Border>
        </Border>

        <Border Background="#BFEDF1FF"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Se incarca datele..." FontSize="26" FontWeight="SemiBold" HorizontalAlignment="Center" />
                <ProgressBar Width="320" Height="10" IsIndeterminate="True" Margin="0,16,0,0" />
            </StackPanel>
        </Border>

        <Border Background="#800F172A"
                Visibility="{Binding IsAdminVisible, Converter={StaticResource BoolToVisibility}}">
            <Border Width="760" Style="{StaticResource CardBorderStyle}" Padding="40" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid>
                    <Grid Visibility="{Binding IsAdminAuthenticated, Converter={StaticResource InverseBoolToVisibility}}">
                        <StackPanel>
                            <TextBlock Text="Autentificare administrator" FontSize="32" FontWeight="Bold" Margin="0,0,0,24" />
                            <StackPanel Orientation="Vertical" Margin="0,0,0,16">
                                <TextBlock Text="Utilizator" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}" />
                                <TextBox Text="{Binding AdminUserInput, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <StackPanel Orientation="Vertical" Margin="0,0,0,24">
                                <TextBlock Text="Parola" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}" />
                                <PasswordBox x:Name="AdminPasswordBox" PasswordChanged="AdminPasswordBox_OnPasswordChanged" />
                            </StackPanel>
                            <TextBlock Text="{Binding AdminErrorMessage}" Foreground="#FFD64545" FontSize="16" Margin="0,0,0,16" />
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Anuleaza" Command="{Binding CloseAdminCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,16,0" />
                                <Button Content="Autentificare" Command="{Binding AuthenticateAdminCommand}" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                    <Grid Visibility="{Binding IsAdminAuthenticated, Converter={StaticResource BoolToVisibility}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Setari kiosk" FontSize="32" FontWeight="Bold" Grid.Column="0" VerticalAlignment="Center" />
                            <Button Content="Delogare" Command="{Binding CloseAdminCommand}" Style="{StaticResource SecondaryButtonStyle}" Grid.Column="1" Margin="20,0,0,0" />
                        </Grid>
                        <Grid Grid.Row="1" Margin="0,24,0,24">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,24,0">
                                <TextBlock Text="Camera" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}" />
                                <ComboBox ItemsSource="{Binding Cameras}" SelectedItem="{Binding SelectedCamera}" DisplayMemberPath="Name" />
                                <TextBlock Text="Imprimanta" Margin="0,24,0,0" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}" />
                                <ComboBox ItemsSource="{Binding Printers}" SelectedItem="{Binding SelectedPrinter}" />
                                <CheckBox Content="Mod blocare kiosk" IsChecked="{Binding IsLockMode}" Margin="0,24,0,0" FontSize="18" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Fisier Excel participanti" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}" />
                                <DockPanel>
                                    <TextBox Text="{Binding ExcelPathInput, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,0" />
                                    <Button Content="Browse" Width="110" Command="{Binding BrowseExcelCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                                </DockPanel>
                                <TextBlock Text="Sablon DOCX ecuson" Margin="0,24,0,0" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}" />
                                <DockPanel>
                                    <TextBox Text="{Binding TemplatePathInput, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,0" />
                                    <Button Content="Browse" Width="110" Command="{Binding BrowseTemplateCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                                </DockPanel>
                                <Button Content="Salveaza" Margin="0,24,0,0" Command="{Binding SaveAdminSettingsCommand}" />
                            </StackPanel>
                        </Grid>
                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Reload Excel" Command="{Binding ReloadExcelCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,16,0" />
                            <Button Content="Inchide" Command="{Binding CloseAdminCommand}" />
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Border>
    </Grid>
</Window>